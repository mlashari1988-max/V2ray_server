version: '3'
services:
  v2ray:
    image: v2fly/v2fly-core:latest
    command: run -c /etc/v2ray/config.json
    volumes:
      - ./config.json:/etc/v2ray/config.json
    ports:
      - "8080:8080"
    restart: always
